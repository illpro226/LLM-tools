# testmap Status

**Archived 2026-08-07** (docs/decisions/0009): not installed, not on PATH,
not in guidance. Code and tests kept as-is for a possible revival; see
0009 for the revival bar. The status below describes the tool as it stood
at archival.

Implemented (v0.1.1, 2026-07-11). Single-file stdlib CLI (`testmap.py`);
23 tests against `tests/fixtures/repo/`. `record` is pytest-only so far
(ADR-007); go/js recorders are roadmap items.
