# testmap Status

Implemented (v0.1.1, 2026-07-11). Single-file stdlib CLI (`testmap.py`);
23 tests against `tests/fixtures/repo/`. `record` is pytest-only so far
(ADR-007); go/js recorders are roadmap items.
