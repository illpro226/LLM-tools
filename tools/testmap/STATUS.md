# testmap Status

Implemented (v0.1.0, 2026-07-10). Single-file stdlib CLI (`testmap.py`);
22 tests against `tests/fixtures/repo/`. `record` is pytest-only so far
(ADR-007); go/js recorders are roadmap items.
