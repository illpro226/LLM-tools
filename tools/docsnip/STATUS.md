# docsnip Status

Scaffold only. No implementation files exist yet.

Deferred indefinitely — the build list is closed
(docs/decisions/0003-close-the-list.md). Docs MCP channels plus
`inspect`/`xread` cover this tool's job. Revival requires a new decision
record citing recorded known-issue evidence of repeated version drift
between docs-channel answers and installed-package behavior.
