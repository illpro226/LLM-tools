"""Math helpers used across the fixture repo."""


def add(a, b):
    """Add two numbers."""
    return a + b


def _scale(x, factor):
    """Private helper, not exported."""
    return x * factor


PI = 3.14159
