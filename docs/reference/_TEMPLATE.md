# <what this maps>

<!--
Reference is the fifth kind, and the only one that is not a record: no number,
no mutability rule, superseded by editing rather than by a successor. It answers
"what is there" - a module map, a route table, a command list, a config
inventory.

It is not a SPEC (which says what must be true and can fail), not a PLAN (how,
ordered), not a DEC (why, append-only), and not an ISS (what is broken). If the
content could return "no", it belongs in one of those instead.

Name it for what it maps: backend-map.md, api-reference.md, csl-reference.md.
No NNNN prefix - there is no graph of cross-references to break, because
reference documents are replaced wholesale rather than superseded.
-->

One sentence: what this document maps, and when an agent needs it.

## <section>

<!-- Tables beat prose. A route table, a module table, a variable table. -->

| <thing> | <what it does> |
|---|---|
|  |  |
